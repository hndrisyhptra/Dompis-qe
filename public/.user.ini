; Batas upload untuk evidence foto teknisi.
; Dibaca PHP-FPM (produksi). Untuk dev `php artisan serve`, samakan di php.ini Homebrew.
; Nginx (produksi) juga perlu: client_max_body_size 60m;
upload_max_filesize = 12M
post_max_size = 60M
max_file_uploads = 40
memory_limit = 256M
